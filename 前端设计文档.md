这份 **AI 游戏自动化开发平台（AI Game Engine Studio）** 的前端详细设计说明书，结合了你提出的 **5 大核心交互阶段**、**Claude Agent SDK 工作流**、**Ali Token Plan 图像生成 pipeline** 以及 **Phaser.js 游戏架构**。

---

# AI 游戏开发平台前端详细设计说明书

## 1. 技术栈选型与 UI 设计规范

### 1.1 技术栈推荐

* **核心框架**：`React 18` + `Vite`（兼顾极速 HMR 与组件化生态）
* **UI 组件库**：`Tailwind CSS` + `Shadcn UI`（高度自订的现代暗黑风界面）
* **编辑器组件**：
* 代码/Markdown 编辑：`Monaco Editor` (@monaco-editor/react)
* Markdown 渲染：`react-markdown` + `remark-gfm`


* **通信与状态**：
* 全局状态：`Zustand`（项目状态、当前 Agent 交互阶段、流式消息缓存）
* 实时通信：`WebSocket` / `SSE`（接收 Agent 任务节点推送与控制台 Logs）


* **图标与视觉**：`Lucide-react`（统一精致矢量图标库）

### 1.2 视觉与主题规范 (Theme System)

* **风格定位**：**Dark Modern Studio Style**（暗黑极客工坊风格，深灰色基底搭配电光青/翡翠绿高亮）。
* **配色方案**：
* 背景色：`#0F172A` (Slate-900) / 卡片背景：`#1E293B` (Slate-800)
* 主色调（Accent）：`#10B981` (Emerald-500，代表 Agent 活跃与代码生成)
* 次色调：`#06B6D4` (Cyan-500，代表素材 Pipeline 与流式响应)
* 警示/错误：`#EF4444` (Red-500，静态语法错误/编译报错)
* 文字主色：`#F8FAFC` (Slate-50) / 次色：`#94A3B8` (Slate-400)



---

## 2. 全局状态与导航控制器 (Global Navigation & State)

### 2.1 顶栏（Global Header & Stage Stepper）

顶栏常驻，显示当前选择的游戏，以及根据后端数据库状态同步的 **5 阶段主流程导航栏**：

```
[ 🎮 AI Game Studio ]  |  [ 📂 当前游戏: 农场物语 ▾ ]  |  [ 📌 阶段: 3. 素材生成 ]
──────────────────────────────────────────────────────────────────────────────────
 Step 1: 头脑风暴 ──► Step 2: 需求/素材定义 ──► Step 3: 素材生成 ──► Step 4: 代码迭代 ──► Step 5: 版本发布
 (Completed)          (Completed)              (Active)           (Locked)          (Locked)

```

| 元素 | 说明 | 交互 / 状态响应 |
| --- | --- | --- |
| **游戏切换下拉框** | 切换当前处于开发中的项目 | 点击切换后，自动加载数据库中对应游戏的 `current_stage` 及其数据 |
| **阶段 Stepper** | 显示 1~5 阶段图谱 | 高亮当前激活阶段；未激活阶段变灰；允许点击**已解锁/已完成**的阶段进行回溯跳转 |
| **状态指示灯** | 显示 Agent 状态 | 🟢 `Agent 思考/开发中` / 🟡 `等待用户输入` / 🔴 `生成中断/错误` |

---

## 3. 页面详细设计

### 3.1 页面一：头脑风暴与项目孵化中心 (Brainstorming Hub)

#### 布局设计

* **左侧：存档项目卡片网格 (Archived Games Grid)**
* **右侧/中央：超级头脑风暴 Agent 对话框 (Superpower Chat Workspace)**

```
┌──────────────────────────────────────┬─────────────────────────────────────────┐
│ 📚 已存档游戏                         │ 🧠 游戏创意头脑风暴 (Superpower Agent)   │
│ ┌──────────────────┐ ┌─────────────┐ │ ┌─────────────────────────────────────┐ │
│ │ 🖼️ 像素农场      │ │ 🖼️ 赛博朋克 │ │ │ Agent: 嗨！你想做一个什么类型的游戏？│ │
│ │ 阶段: 代码生成    │ │ 阶段: 设计  │ │ │ 用户: 我想做一款肉鸽塔防游戏        │ │
│ └──────────────────┘ └─────────────┘ │ │ Agent: 听起来不错！核心机制是...    │ │
│ [ ➕ 新建游戏创意 ]                  │ └─────────────────────────────────────┘ │
│                                      │ [ 💡 提示词输入框...           ] [发送]│
└──────────────────────────────────────┴─────────────────────────────────────────┘

```

#### 关键交互逻辑

1. **新建创意**：点击 `➕ 新建游戏创意`，右侧打开对话窗口。
2. **Superpower 多轮对话**：
* Agent 根据 Kimi K3 提问（包含游戏类型、核心玩法、胜利条件、美术风格）。
* 前端支持 **交互选项组件 (Option Chips)**：当 Agent 输出带选项时，前端渲染可点击按钮（如 `[选项 A: 2D 像素风]` `[选项 B: 手绘卡通风]`）。


3. **确认完毕落地阶段**：
* 当 Agent 评估需求完整后，对话末尾弹出高亮卡片：`已生成 <游戏名>-game-design.md`。
* 同时显示大按钮：`👉 前往确认需求与素材清单`（点击跳转到阶段 2）。



---

### 3.2 页面二：需求与美术素材协同编辑页 (Design & Spec Review)

#### 布局设计：双栏文档编辑器 + AI 协同 Copilot

* **顶部 Tabs**：`[ 📄 1. game-design.md ]` | `[ 🎨 2. 美术素材.md ]`（初始锁定 Tab 2，确认 Tab 1 后解锁 Tab 2）
* **主区域**：分栏界面（左侧 Markdown 实时编辑/渲染，右侧 AI 交互修改框）

```
┌─────────────────────────────────────────────────────────┬──────────────────────┐
│  Tabs: [ 📄 game-design.md (已确认) ] [ 🎨 美术素材.md ]│ 🤖 AI 文档修饰 Copilot │
├─────────────────────────────────────────────────────────┼──────────────────────┤
│ Monaco Markdown Editor                                  │ ┌──────────────────┐ │
│ # 美术素材清单                                           │ │ 💡 交互提示      │ │
│ ## 1. 角色与 NPC                                        │ │ "觉得角色数量太小│ │
│ - [hero_idle]: 主角站立图, 纯色背景, 32x32 ...           │ │ 可以让 AI 补充 3 │ │
│ - [monster_slime]: 史莱姆怪物 ...                       │ │ 个近战怪物"      │ │
│                                                         │ └──────────────────┘ │
│                                                         │ 输入微调指令...      │
│ [ 💾 保存文档 ]                       [ ✅ 确认并生成 ] │ [ 📤 发送微调指令 ] │
└─────────────────────────────────────────────────────────┴──────────────────────┘

```

#### 关键交互逻辑

1. **阶段 2.1 (game-design.md)**：
* 用户可在 Monaco 编辑器中自由更改 Markdown。
* 支持在右侧输入 AI 调整指令（如 *"加入一个夜间模式逻辑"*），Agent 修改 Markdown 并重新呈现。
* 点击 `[ ✅ 确认 Design.md ]` 后，保存文件至 `docs/<游戏名>-game-design.md`，自动触发展示并加载 `美术素材.md`。


2. **阶段 2.2 (美术素材.md)**：
* Agent 根据 Design.md 拆解资产清单并填充 Markdown。
* 确认无误后点击 `[ 👉 进入素材生成看板 ]`（进入阶段 3）。



---

### 3.3 页面三：AI 素材生成与抠图工坊 (Asset Studio & Matting Pipeline)

#### 布局设计：顶部工具栏 + 分类 Tab + 素材卡片网格 + 风格转绘 Drawer

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ 分类: [ 全部 (12) ] [ 🖼️ 背景 (2) ] [ 🧙 角色/NPC (6) ] [ 🧱 道具/UI (4) ]     │
├────────────────────────────────────────────────────────────────────────────────┤
│ 批量操作: [ 🔘 全选 ] [ 🪄 批量生成选中 ] [ ✂️ 批量抠图 ]  | [ 🎨 打开风格转绘面板 ] │
├────────────────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────┐ ┌──────────────────────┐ ┌──────────────────────┐ │
│ │ 🧙 hero_idle        │ │ 👾 monster_slime     │ │ 🌄 level1_bg         │ │
│ │ 🏷️ 角色              │ │ 🏷️ 角色              │ │ 🏷️ 背景              │ │
│ │ ┌──────────────────┐ │ │ ┌──────────────────┐ │ │ ┌──────────────────┐ │ │
│ │ │ 🖼️ 透明 PNG 预览  │ │ │ 🖼️ 原始图片      │ │ │ 🖼️ 未生成          │ │ │
│ │ └──────────────────┘ │ │ └──────────────────┘ │ │ └──────────────────┘ │ │
│ │ [ Status: 已抠图 ]   │ │ [ Status: 未抠图 ]   │ │ [ Status: 待生成 ]   │ │
│ │ Prompt: 勇者站立...  │ │ Prompt: 绿史莱姆...  │ │ Prompt: 森林背景...  │ │
│ │ [🎲重新生成][✂️抠图] │ │ [🎲重新生成][✂️抠图] │ │ [🎲生成图片]         │ │
│ └──────────────────────┘ └──────────────────────┘ └──────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────┘

```

#### 核心操作卡片 (Asset Card) 组件规范

每个素材卡片包含以下元素与状态：

* **状态标签 (Badge)**：`待生成` / `生成中 (Spinner)` / `已生成 (Raw)` / `已抠图 (Transparent)`
* **图片对比视图**：支持点击切换看 `raw/` 还是 `processed/` 抠图透明结果。
* **快捷按钮组**：
* `[🎲 生成 / 重新生成]`：调用 Token Plan (`wan2.7-image`) API。
* `[✂️ AI 抠图]`：调用 `imgly/rembg` 接口，自动去除背景转换为透明 PNG，存入 `assets/processed/`。
* `[✏️ 修改 Prompt]`：弹窗修改该素材的 Prompt、透明度及尺寸要求。


* **侧边“风格转绘”抽屉面板 (Style Transfer Panel)**：
* 允许用户上传 `风格参考图` 与 `结构参考图`。
* 选中要转绘的单张或多张素材，调用图生图/风格转绘 API。



---

### 3.4 页面四：代码增量生成与 Web 试玩工作区 (Code Iteration & Live Playground)

这是最核心的代码开发与调试界面，采用现代 IDE 架构风格。

#### 布局设计：四区域协同

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ 阶段进度: [ Spec 拆分 ] ──► [ 💻 代码编写/自检 ] ──► [ 🕹️ Web 试玩 ] ──► [ 📦 模块完成 ]│
├──────────────┬──────────────────────────────────────────┬──────────────────────┤
│ 📂 文件树    │ 📝 代码编辑与 Spec 预览 (Monaco/Markdown) │ 🕹️ 游戏实时预览 /    │
│              ├──────────────────────────────────────────┤    反馈 Console      │
│ ▾ src        │ // File: src/scenes/WorldScene.js        │ ┌──────────────────┐ │
│   ▸ config   │ class WorldScene extends Phaser.Scene {  │ │ 🎮 Phaser.js     │ │
│   ▾ scenes   │   create() {                             │ │    Web Canvas    │ │
│     Boot.js  │     this.add.image(400, 300, 'bg');      │ │    (Running...)  │ │
│     World.js │   }                                      │ └──────────────────┘ │
│   ▸ entities │ }                                        │ ┌──────────────────┐ │
│   assets_map │                                          │ │ 💬 Coder Agent    │ │
│              │                                          │ │ 反馈与修改意见:  │ │
│ ─────────────────────────────────────────────────────── │ │ "角色移动速度太快│ │
│ ⚙️ 模块控制:  │ 终端日志 (Terminal Logs):                │ │ 请调整"          │ │
│ 当前模块: V1 │ > Headless browser syntax check... PASS! │ │ [ 📤 发送修补 ]  │ │
│ MVP 最小交付 │ > Local Server running at http://...    │ └──────────────────┘ │
└──────────────┴──────────────────────────────────────────┴──────────────────────┘

```

#### 四大核心区块详细说明

1. **顶栏 Spec 与状态导航条 (Stage Controller)**
* 显示阶段状态：`Spec 拆分` ➔ `代码编写` ➔ `无头浏览器自检 (Headless Check)` ➔ `试玩与用户反馈` ➔ `保存 Git Tag`。


2. **左侧：游戏代码结构树 (File Explorer)**
* 树状结构同步展现本地 `game-project-root/src/` 目录结构（包含 `scenes/`, `entities/`, `models/`, `assets_map.js` 等）。


3. **中间：代码/Spec 视图 (Editor & Terminal Hub)**
* **上半部分**：Monaco Editor，支持 JS/HTML/JSON 语法高亮、查看与手动代码修改。
* **下半部分**：Agent 命令行控制台日志（WebSocket 实时输出 Coder Agent 的思考、语法自检过程、Headless Browser 的 Console.Error 捕捉状态）。


4. **右侧：嵌入式 Web 试玩与修改反馈 Dock (Playground & Feedback)**
* **实时 Preview Iframe**：嵌入预览服务器（如 `http://localhost:3000`）链接，支持全屏与刷新按钮。
* **反馈交互输入框**：
* 试玩有 BUG 或不符合预期时，用户直接在输入框写下意见（如 *"碰撞体积有问题，怪物会穿墙"*）。
* 点击 `[发送修改意见]`，重新触发 Coder Agent 进入 **静态自检 ➔ 无头自检 ➔ 重新部署** 的闭环。


* **版本归档按钮**：试玩满意后，点击 `[ 💾 保存为当前版本 (Git Tag) ]`。



---

### 3.5 阶段五/全局辅助：Git 版本管理与回滚面板 (Version Management Modal/Drawer)

可以通过页面四顶栏的 `[ 📜 版本历史 ]` 按钮调出此面板。

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ 📜 项目 Git 版本 checkpoint 记录                                                │
├────────────────────────────────────────────────────────────────────────────────┤
│  Tag 名称             提交信息                           时间       操作        │
│ ────────────────────────────────────────────────────────────────────────────── │
│  v0.3-battle-system   feat(module): 战斗与伤害系统测试通过  10:42 AM   [ 🔄 回滚 ] │
│  v0.2-map-load        feat(module): 地图与资产加载完成      10:15 AM   [ 🔄 回滚 ] │
│  v0.1-mvp-init        feat(module): MVP 基础场景挂载        09:30 AM   [ 🔄 回滚 ] │
└────────────────────────────────────────────────────────────────────────────────┘

```

---

## 4. 前后端通信协议与状态机流转设计

### 4.1 阶段状态机 (State Machine)

全局维护 `GameStateContext`，包含数据库状态定义：

```ts
type EngineStage = 
  | 'STAGE_1_BRAINSTORM'    // 头脑风暴与落地
  | 'STAGE_2_DESIGN_DOC'    // Design.md & 美术素材.md 确认
  | 'STAGE_3_ASSET_PIPELINE'// 美术资产 API 生成与抠图
  | 'STAGE_4_CODE_ITERATION'// 增量 Spec 开发与试玩
  | 'STAGE_5_COMPLETED';    // 全部开发完成

```

### 4.2 WebSocket 消息类型定义

前端通过 WebSocket 接收 Agent Orchestrator 实时推送的数据包：

```json
// 1. Agent 文本流式思考/回复
{
  "type": "AGENT_STREAM_CHUNK",
  "stage": "STAGE_1_BRAINSTORM",
  "content": "建议增加一个 NPC 商店机制，你觉得呢？",
  "options": ["同意添加", "暂不需要"]
}

// 2. Headless 语法检查报错反馈 (STAGE 4)
{
  "type": "CODE_CHECK_STATUS",
  "status": "SYNTAX_ERROR" | "HEADLESS_PASSED" | "HEADLESS_FAILED",
  "logs": "[SyntaxError] Unexpected token '}' in src/scenes/WorldScene.js line 42",
  "autoFixing": true
}

// 3. 实时游戏预览 URL 就绪事件
{
  "type": "PREVIEW_READY",
  "previewUrl": "http://localhost:8080/?session=game_farmer_v1",
  "versionTag": "v0.1-mvp"
}

```

---

## 5. 前端组件矩阵与文件目录结构建议

为了方便前端开发实施，推荐的项目代码目录架构如下：

```
src/
├── assets/                  # 前端静态图标、默认占位图
├── components/              # 基础 UI 组件 (Shadcn UI)
│   ├── ui/                  # Button, Card, Tabs, Dialog, Drawer 等
│   └── shared/              # Header, Stepper, StatusBadge
├── features/                # 5 大核心功能模块页面组件
│   ├── 1-brainstorm/        # ArchivedGameGrid, SuperpowerChat
│   ├── 2-design/            # MarkdownDualEditor, SpecCopilot
│   ├── 3-assets/            # AssetCard, BatchToolbar, StyleTransferDrawer
│   ├── 4-coder/             # FileTree, MonacoContainer, LivePreviewIframe, FeedbackDock
│   └── 5-versions/          # GitVersionHistoryModal
├── hooks/                   # 自定义 Hooks
│   ├── useAgentWebSocket.ts # Agent 交互与流式监听
│   └── useProjectState.ts   # 项目持久化与阶段控制
├── store/                   # Zustand 全局状态
│   └── useGameStore.ts
└── services/                # REST API 接口 (请求 Token Plan, KSPMAS, RMBG)

```

---

## 6. 总结

本前端设计完全遵循你的后端架构与 Agent 分工理念：

1. **统一性**：暗黑现代工坊风格，适配复杂的 Markdown、图像看板与 IDE 式界面。
2. **状态流转透明**：从创意对话（Superpower）到 `game-design.md`，再到 Asset Pipeline 图像生成与 Phaser.js 代码开发，每一个阶段均有明确的 UI 状态引导与人工干预/确认机制 (Human-in-the-Loop)。
3. **闭环体验**：前端把无头浏览器检测日志与实时试玩反馈集成到右侧 Dock 栏，能让开发者无缝进行“试玩 - 提出反馈 - Agent 修复 - 刷新试玩 - 提交 Git Tag”的无缝闭环。